tStep = 0.1  # time step (unitless)
dragC = 0.5  # drag coefficient (unitless)

WIDTH = 600  # window width (pixels)
HEIGHT = 600  # window height (pixels)

knowMass = 1  # mass of a knowledge base (unitless)
knowRadius = 0.02  # radius of a knowledge base (fraction of window)

resMass = 2  # mass of a resource base
resRadius = 0.04  # radius of a resource base (fraction of window)

conWidth = 0.005  # width of a connector line (fraction of window)
conStrength = 1  # strength of a spring (unitless)
conEqLength = 0.3  # equilibrium length of a spring (unitless)
